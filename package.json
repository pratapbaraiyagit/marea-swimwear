{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "lsof -ti:5173 | xargs kill -9 2>/dev/null; cd artifacts/marea-swimwear && PORT=5173 BASE_PATH=/ node_modules/.bin/vite --config vite.config.ts --host 0.0.0.0",
    "start": "lsof -ti:5173 | xargs kill -9 2>/dev/null; cd artifacts/marea-swimwear && PORT=5173 BASE_PATH=/ node_modules/.bin/vite --config vite.config.ts --host 0.0.0.0",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.3"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
